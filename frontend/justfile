default:
    @just --list

# Builds the frontend application
dev:
    @npm run dev

# Builds the frontend application
build:
    @npm run build

# Builds the spacetimedb backend in debug mode
build-debug:
    @npm run build-debug

# Deploys the application a location
deploy:
    @echo "TODO implement me!"
